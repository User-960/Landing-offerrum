/* Root elements */
const ROOT_HEADER = document.querySelector('.header')
const ROOT_FOOTER = document.querySelector('.footer')
const ROOT_BENEFITS = document.querySelector('.benefits__box')

const ROOT_FORM = document.querySelector('.form')
const ROOT_SPINNER = document.querySelector('.spinner')

const ROOT_TOP_BTN = document.querySelector('.top__btn')
